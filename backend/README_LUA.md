Lua demo

This workspace is primarily Python/JS. For fun, there's a tiny Lua script `fun.lua` in the `backend` folder.

Run (if you have Lua installed):

```bash
lua fun.lua
```

It prints a small ASCII summary of sample features and their sentiment split.
